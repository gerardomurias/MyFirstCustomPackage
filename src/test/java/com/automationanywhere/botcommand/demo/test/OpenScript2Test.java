package com.automationanywhere.botcommand.demo.test;

import com.automationanywhere.botcommand.demo.OpenScript;
import java.util.HashMap;

public class OpenScript2Test {

    public static void main (String[] args){

        /*OpenScript action = new OpenScript();
        HashMap<String,Object> mySession = new HashMap<String,Object>();

        // Instantiate the object that represents the content of your session

        mySession.put("Default",null);// replace null with the object representing your session content

        action.setSessionMap(mySession);

        action.open("Default", "Script", "3");*/
    }
}
